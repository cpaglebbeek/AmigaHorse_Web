---
date: 2026-05-31
repo: AmigaHorse_Web
status: open
resume: "verder met AmigaHorse_Web v0.0.2.x sub-step 5 — npm install + npm run build:wasm + npm run dev draaien, smoke-test in browser bewijzen; daarna user-supplied KS 1.3 + WB 1.3 ADF + AmigaBASIC uploaden via /basic/setup.html, warm-snapshot-bake-flow implementeren (sequence: powerOn → loadFile WB → boot → schedule keys naar AmigaBASIC-icon → save), amiga-keymap.js maken voor LOAD/RUN sequence, end-to-end HELLO WORLD .bas test"
---

# Sessie 2026-05-31 — AmigaHorse_Web v0.0.2-CannonFodder (BASIC-modus + vAmigaWeb-basis)

**Agent:** Claude Opus 4.7 (1M context)
**Repo:** AmigaHorse_Web (`cpaglebbeek/AmigaHorse_Web`)
**Branche:** main
**Codenaam:** Cannon Fodder
**Versie-bump:** 0.0.1-Turrican → 0.0.2-CannonFodder (Oranje per CLAUDE.md — nieuwe Core-binding + nieuwe routes + nieuwe IndexedDB-stores)
**Cross-repo werk:** Meta_AmigaHorse (P-AMH-09 + ARCHITECTURE + v0.1.0-Worms bump), Meta_Master (PROJECTS.json versies + STATUS + memory + RESUME)

---

## Opdracht (samengevat)

Gebruiker resumeerde Web met scope-uitbreiding: naast Full configurable ook een **Quick BASIC mode** voor zelfgemaakte `.bas`-bestanden. Gelicenseerde binaries (KS 1.3 + WB 1.3 ADF + AmigaBASIC) levert gebruiker zelf aan. Doel: ~3-5 sec van drop tot RUN.

---

## Prompts en acties — chronologisch

### Prompt 1 — "verder met AmigaHorse_Web v0.0.2; let op: ik wil naast de full configurable experience een snelle manier op zelfgemaakte amigabasic, bas bestanden te kunnen laden/starten. Alle benodigde gelicenseerde binaries kan ik aanleveren"

**Actie:** Multi-session check + WhatIf met 7 vragen (BASIC-scope / auto-RUN / file-injection / Core-keuze / asset-upload / routes / warm-snapshot). Architectuurvoorstel: twee parallelle routes, vAmigaWeb-basis, warm-snapshot-flow, hostfs-injection.

### Prompt 2 — "akkoord op alle 7, ga bouwen"

**Actie:** Stap 1-7 uitgevoerd in één sessie.

---

## Beslissingen (definitief v0.0.2)

| # | Vraag | Besluit |
|---|---|---|
| 1 | BASIC-scope | AmigaBASIC only v0.0.2; AMOS in v0.0.3+ |
| 2 | Auto-RUN | Default ON, toggle "stop in BASIC-prompt" voor LIST/edit |
| 3 | File-injection | Hostfs via Emscripten MEMFS ↔ vAmiga `mountDH` (verifieren in v0.0.2.x); fallback = on-the-fly ADF-rebuild |
| 4 | Core-basis | vAmigaWeb (GPL-3.0 → AGPL-3.0 upgrade); coherentie X86/Android in v0.0.3 |
| 5 | Asset-upload | 3 file-pickers v0.0.2 (KS / WB / AmigaBASIC); bundle-zip v0.0.3 |
| 6 | Routes | `/` = Quick BASIC, `/full` = configurable, `/basic/setup` = wizard |
| 7 | Warm-snapshot | Pre-warm one-time + cache in IndexedDB |

## Wijzigingen v0.0.2

### Meta_AmigaHorse (v0.0.1-Lemmings → v0.1.0-Worms, Oranje)
- `docs/PRINCIPLES.md`: **P-AMH-09 toegevoegd** (BASIC as first-class use-case)
- `ARCHITECTURE.md`: Variant 1 Web uitgebreid met twee routes + warm-snapshot data-flow + vAmigaWeb besluit
- `CLAUDE.md`: codenaam-toewijzingen Meta v0.1.0-Worms + Web v0.0.2-CannonFodder; pool aangepast (Cannon Fodder + Worms verwijderd uit "vrije pool")
- `VERSION` + `CHANGELOG.md`

### AmigaHorse_Web (v0.0.1-Turrican → v0.0.2-CannonFodder, Oranje)

Updates:
- `VERSION`, `CHANGELOG.md`, `README.md`, `CLAUDE.md`, `ARCHITECTURE.md`, `.gitignore` (ADF/HDF/AmigaBASIC patterns)

Nieuw:
- `docs/BASIC_MODE.md` — Volledige BASIC-flow + AmigaBASIC-historie + KS 2.0+-incompatibility + asset-bundle-vereisten + v0.0.3-roadmap
- `DESIGN_TOKENS.md` — Workbench 1.3 stijl (kleuren, typografie, spacing, breakpoints, componenten)
- `package.json` — vanilla JS + esbuild devDep
- `esbuild.config.mjs` — bundling-config met 4 entry-points
- `external/README.md` — vAmigaWeb submodule-instructies + build-pipeline
- `src/index.html` — route-picker landing
- `src/styles.css` — basis-styles met Amiga-tokens
- `src/wasm-bridge.js` — typed wrapper rondom vAmiga-WASM (stub) + IndexedDB-helpers
- `src/basic/index.html` + `quick-launch.js` — Quick BASIC dropzone-route met auto-RUN toggle
- `src/basic/setup.html` + `setup.js` — 4-step Asset-Setup-wizard
- `src/full/index.html` + `library.js` — Full mode skelet

Status v0.0.1 sessie-MD: `done` (deze sessie sluit hem af).

## v0.0.2.x roadmap (5 sub-steps)

- [x] **Sub-step 1 (klaar 31-05, v0.0.2.1)** — vAmigaWeb submodule add via `git submodule add https://github.com/vAmigaWeb/vAmigaWeb.git external/vamigaweb`, pinned `c3c50d9` (geen tags upstream); URL-correctie `dirkwhoffmann/vAmigaWeb` → `vAmigaWeb/vAmigaWeb` in 7 files; UPSTREAM_AUDIT.md baseline opgesteld; licentie geverifieerd GPL-3.0 (vAmigaWeb) + 3-tier vAmiga (GPL-3.0+/MPL-2.0/MIT) — alle AGPL-3.0-compat.
- [x] **Sub-step 2 (klaar 31-05, v0.0.2.2)** — Emscripten SDK 5.0.7 geïnstalleerd op `~/Documents/Gemini_Projects/emsdk` (1.8 GB), bundled node 22.16.0 + python 3.13.3 + wasm-binaries `6cd98e86d`. Helper-script `tools/emscripten-env.sh` aangemaakt (sourceable on-demand, géén `.zshrc`-aanpassing). `emcc --version` geverifieerd: `5.0.7 (263db4cf...)`. Pin op 5.0.7 voor v0.0.2.x.
- [x] **Sub-step 3 (klaar 31-05, v0.0.3-Flashback)** — WASM-build pipeline werkt end-to-end via `tools/build-wasm.sh`. Output: `dist/vendor/vamigaweb/vAmiga.js` (106 KB Emscripten glue) + `vAmiga.wasm` (8.77 MB Amiga-emulator binary). Build-tijd cold-cache ~3-4 min, warm-cache ~1 min. **VERSION-bump 0.0.2-CannonFodder → 0.0.3-Flashback** ("geen build zonder bump"). Confirmaties: (1) géén `_wasm_mountDH` in exports → fallback ADF-rebuild bevestigd voor BASIC-injection, (2) nonworker-mode default → géén COOP+COEP-headers verplicht, (3) `_wasm_save_workspace/_wasm_load_workspace/_wasm_auto_type/_wasm_loadFile` zijn allemaal beschikbaar → alle puzzelstukken voor BASIC-flow aanwezig.
- [x] **Sub-step 4 (klaar 31-05, v0.0.4-Speedball2)** — Concrete `cwrap`-bindings in `src/wasm-bridge.js` voor `run`/`halt`/`reset`/`powerOn`/`configure`/`key`/`scheduleKey`/`loadFile`. Promise-based `init()` met `Module.onRuntimeInitialized`-callback (classic Emscripten load-pattern). `saveStateToBuffer/restoreStateFromBuffer` via Emscripten-FS. **`src/lib/build-blank-adf.js`** nieuw: pure-JS OFS-ADF-builder (boot+root+bitmap+file-header+data-block, correcte checksums, Amiga-string-encoding, amigaHash). Limiet v0.0.4: 488 bytes per file (1 OFS data-block). **`esbuild.config.mjs` herschreven** met dev-server (npm run dev → :8000 + watch) en productie-bundle. Smoke-test-knop op `/basic/` voor live module-load-bewijs. `node --check` op alle JS-files passt. **Bevestigde keuzes:** wasm_auto_type is obsolete → `wasm_schedule_key` is de juiste keyboard-API; vAmiga's `load_disk()` auto-detect file-types op header-bytes, dus géén type-flag nodig. **Niet getest in browser yet** — sub-step 5.
- [ ] **Sub-step 5** — `npm install` + `npm run build:wasm` + `npm run dev`, smoke-test in browser bewijzen. Daarna user-supplied KS 1.3 + WB 1.3 ADF + AmigaBASIC binary uploaden via `/basic/setup.html`, warm-snapshot-bake-flow implementeren in `src/basic/setup.js` (sequence: powerOn → loadFile WB → wait boot → schedule keys naar AmigaBASIC-icon → wait BASIC-prompt → save workspace → IndexedDB). `src/lib/amiga-keymap.js` aanmaken (ASCII → Amiga rawkey-code mapping). End-to-end HELLO WORLD `.bas`-test.

## Trigger-zin voor /checkresume

"verder met AmigaHorse_Web v0.0.2.x sub-step 5 — npm install + npm run dev, smoke-test bewijzen, daarna warm-snapshot-bake + amiga-keymap + end-to-end HELLO WORLD .bas"

## Risico's / open punten

- `vAmiga.mountDH` API mogelijk niet aanwezig in vAmigaWeb → fallback naar ADF-rebuild (te verifieren in sub-step 4)
- COOP+COEP-hosting nog niet geregeld (Cloudflare Pages / Netlify / HC55-nginx-vhost te kiezen v0.0.2.x)
- AmigaBASIC binary lokaal beschikbaar? Gebruiker zegt ja
- ~~vAmigaWeb upstream-licentie-audit per-file bij eerste merge (P-AMH-07)~~ → baseline gedaan 31-05 in `Meta_AmigaHorse/docs/UPSTREAM_AUDIT.md`, per-file-spider in sub-step 3

## Vervolg-log

### Vervolg 4 — 2026-05-31 v0.0.4-Speedball2 — cwrap-bindings + ADF-builder + dev-pipeline (sub-step 4)

Gebruiker prompt: "verder met sub-step 4". WhatIf met 6 punten gepresenteerd (wasm-bridge / OFS-builder / esbuild dev-server / HTML script-tag / VERSION-bump / beperkingen). Akkoord op "akkoord, ga bouwen".

Onderzoek vóór bouwen:
1. `head -30` van `dist/vendor/vamigaweb/vAmiga.js` → classic Emscripten Module-globaal (NIET ES-module). Loading-pattern: `window.Module = {...}` vóór script-tag-inject, dan await onRuntimeInitialized.
2. `grep` op main.cpp signaturen:
   - `wasm_loadFile(char* name, u8* blob, long len, u8 drive_number) → const char*`
   - `wasm_schedule_key(int code1, int code2, int pressed, int frame_delay) → void`
   - `wasm_save_workspace(char* path) → char*` (schrijft naar Emscripten-FS-pad)
   - `wasm_load_workspace(char* path) → void`
   - `wasm_power_on(unsigned) → const char*`
3. `wasm_auto_type` is **obsolete** (commentaar in main.cpp) → keyboard via `wasm_schedule_key`
4. `_wasm_loadFile` roept `load_disk(name, blob, len)` → vAmiga auto-detect file-type op header-bytes → geen extra type-flag nodig
5. Drive numbering: 0=df0 (boot), 1=df1, ... → DF1: voor onze .bas-ADF

Acties:
1. **`src/wasm-bridge.js` herschreven** (~230 regels): Promise-based init met script-tag-inject, 8 cwrap-bindings, manual _malloc voor buffer-passing in loadFile, FS.readFile/writeFile-wrappers voor workspace save/restore.
2. **`src/lib/build-blank-adf.js` aangemaakt** (~280 regels): pure-JS OFS-ADF-builder. Boot block + Root block (sec 880, ht_size=72) + Bitmap (sec 881, bit-mapping inverted) + File-header (sec 882) + OFS-Data-block (sec 883, 488 bytes payload). Correcte sum-mod-2^32-negated checksums. Amiga-string-encoding + amigaHash. v0.0.4-limiet: 488 bytes.
3. **`esbuild.config.mjs` herschreven** met dev-mode (`--dev` flag) en productie-mode. Context.serve op :8000 met watch + onRequest-logging. 5 entry-points. Copy static assets + warning bij ontbrekende `vAmiga.wasm`.
4. **`package.json` scripts** ge-update: `dev`/`build` via `node esbuild.config.mjs`, nieuwe `build:all` (build:wasm + build).
5. **`src/basic/index.html`** → smoke-test-knop "Smoke-test: vAmiga.wasm laden".
6. **`src/basic/quick-launch.js`** → echte API-import + ADF-bouw bij drop (toont byte-count); volledige restore+loadFile+scheduleKey-sequence in sub-step 5.
7. **`node --check`** op 5 JS-files + esbuild.config.mjs + package.json — alles OK.
8. **VERSION-bump 0.0.3-Flashback → 0.0.4-Speedball2** (Bitmap Brothers 1990, action-sports — fitting infrastructuur-acceleratie). Groen +0.0.1 (JS-wiring zonder arch-wijziging). Oranje +0.1.0 spaar ik voor sub-step 5.
9. Meta_AmigaHorse `CLAUDE.md` codename-pool: Speedball 2 toegewezen Web v0.0.4.

Niet getest in browser yet — dat is sub-step 5 (vereist `npm install` + serving).

### Vervolg 3 — 2026-05-31 v0.0.3-Flashback — eerste WASM-build (sub-step 3)

Gebruiker prompt: "verder met sub-step 3 — WASM-build".

Acties:
1. vAmigaWeb's CMakeLists.txt + README ingelezen. Build-systeem: C++20, -O3 -flto, wasm-exceptions, `-sUSE_ZLIB=1`, `-lidbfs.js` (IndexedDB-FS al ingebakken), thread_type=nonworker (default).
2. Exports-lijst geanalyseerd uit `EXPORTED_FUNCTIONS`: ~60 `_wasm_*`-functies incl. `_wasm_loadFile`, `_wasm_save_workspace`, `_wasm_load_workspace`, `_wasm_auto_type`, `_wasm_joystick`, `_wasm_pixel_buffer`. **Bevestigd: géén `_wasm_mountDH` of hostfs-API** → sub-step 4 zal ADF-rebuild gebruiken.
3. `.gitignore` uitgebreid voor vAmigaWeb-build-uitvoer in submodule-root (CMakeLists kopieert artefacten naar root via custom targets — die houden we uit git).
4. Eerste cmake-configure-attempt faalde door subshell-pipe-bug (source emsdk_env.sh in subshell verloor PATH). Fix: hertest zonder pipe, `which emcc emcmake` bevestigt actief.
5. **`emcmake cmake -B build -DCMAKE_BUILD_TYPE=Release`** succesvol (6.3 sec). ZLIB-not-found-warning is benign (emcc gebruikt eigen `-sUSE_ZLIB=1` Emscripten-port).
6. **`cmake --build build -j 8`**: bouwt VACore + main + linkt naar `vAmiga.{html,js,wasm}` + `sw.js`. Eerste run genereerde sysroot-libs (libc, libc++, libcompiler_rt, ...): ~3-4 min cold-cache.
7. Artefacten: `vAmiga.html` 65 KB (ignored), `vAmiga.js` 106 KB Emscripten glue, **`vAmiga.wasm` 8.77 MB Amiga-emulator binary**. Build-dir 92 MB.
8. `tools/build-wasm.sh` script geschreven: idempotente pipeline (source emsdk → emcmake configure als nodig → cmake --build → copy naar `dist/vendor/vamigaweb/`).
9. Bug-fix in `tools/emscripten-env.sh`: `${AMIGAHORSE_VERBOSE_EMSDK}` → `${AMIGAHORSE_VERBOSE_EMSDK:-}` (default-empty pattern voor `set -u`-compat).
10. Herhaalde build-test via script: artefact-copy bevestigd. Build-tijd warm-cache: ~1 min.
11. `package.json` `build:wasm` wijst nu naar `bash tools/build-wasm.sh` (echte werkende pipeline).
12. `src/wasm-bridge.js` stubs gelabeld met concrete export-namen uit CMakeLists. `crossOriginIsolated`-check soft gemaakt (alleen-bij-worker-mode).
13. **VERSION-bump 0.0.2-CannonFodder → 0.0.3-Flashback** (Groen +0.0.1: pipeline-uitvoer, geen nieuwe architectuur). Codenaam Flashback (Delphine 1992, rotoscope-action, fitting eerste compile-doorbraak).
14. Meta_AmigaHorse `CLAUDE.md` codename-pool bijgewerkt: Flashback verwijderd uit "vrije pool", toegevoegd aan "toegewezen Web v0.0.3".

### Vervolg 2 — 2026-05-31 v0.0.2.2 — Emscripten install

Gebruiker prompt: "verder met sub-step 2 — Emscripten install".

Acties:
1. Prereq-check: Python 3.14.3, cmake 4.3.3, git 2.53 — alle aanwezig. 382 GB free op disk. Geen bestaande emsdk in PATH.
2. `git clone https://github.com/emscripten-core/emsdk.git` → `~/Documents/Gemini_Projects/emsdk` (buiten alle repo's)
3. `./emsdk install latest` → SDK 5.0.7 (commit `263db4cf...`) + node 22.16.0 + python 3.13.3 + wasm-binaries `6cd98e86d`. 1.8 GB.
4. `./emsdk activate latest` → klaar voor source.
5. `tools/emscripten-env.sh` aangemaakt: sourceable wrapper die emsdk_env.sh stil source't en versie print. Géén `.zshrc`-modificatie (P-AGT scope-isolatie).
6. End-to-end smoke-test: `source tools/emscripten-env.sh && emcc --version` → `Emscripten 5.0.7` ✓
7. `package.json` build:wasm-stub update wijst nu naar sub-step 3.
8. `external/README.md` build-pipeline-sectie toegevoegd met cmake-skelet voor sub-step 3.

Versie blijft 0.0.2-CannonFodder (toolchain-install, geen build-artefact).

### Vervolg 1 — 2026-05-31 v0.0.2.1 — submodule add

Gebruiker prompt: "verder met v0.0.2.x — vAmigaWeb submodule add".

Acties:
1. Multi-session check (Meta_Master pull OK; Web clean; geen parallelle sessie)
2. Upstream-check via `gh api`: ontdekt dat correcte URL `vAmigaWeb/vAmigaWeb` is (niet `dirkwhoffmann/vAmigaWeb` zoals in v0.0.2 docs aangenomen). vAmigaWeb LICENSE = GPL-3.0 geverifieerd. vAmigaWeb 8.6 MB, geen tags/releases. vAmiga 3-tier licentie (GPL-3.0+/MPL-2.0/MIT) — alle AGPL-3.0-compat.
3. `git submodule add` op commit `c3c50d9` (HEAD 28-05). 15 MB lokaal incl. `.git/`.
4. Sed-replace `dirkwhoffmann/vAmigaWeb` → `vAmigaWeb/vAmigaWeb` in 7 files.
5. `Meta_AmigaHorse/docs/UPSTREAM_AUDIT.md` aangemaakt met baseline-entry 1 (vAmigaWeb).
6. `Meta_AmigaHorse/docs/DEPENDENCIES.md` vAmigaWeb-licentie TBC → geverifieerd; vAmiga 3-tier subcomponent toegevoegd.
7. `external/README.md` URL + pin-info + audit-link bijgewerkt.
8. CHANGELOG v0.0.2.1-entry.

Versie blijft `0.0.2-CannonFodder` — `0.0.2.x` is sub-step-traject zonder release-bump tot WASM-build draait.
